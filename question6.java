public class question6{
	/*Codigo:
	 * p=0
       for(i=1;p<=n;i++) {
          p = p+i;
      }
    */
	
	/**
	 * Descripcion:
	 * La primera linea es la inicializacion de la variable p
	 * El ciclo for donde se inicializa al con contador con el valor de 1 y no dicen que p s¡deber ser menor igual a n
	 * El contador se seguira incrementando, y al mismo tiempo el valor de p ira aumentando juntado al contador i.
	 
	 * Tiempo de Complejidad:
	 * Es O(n^(1/2))
	 * **/
}
