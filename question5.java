public class question5{
	/*Codigo:
    for(i=0;i<n;i++) {
       for(j=0;j<i;j++)
  	 //statement
    }
  */
	
	/**
	 * Descripcion:
	 * Estos continuan siendo for anidados con los contadores a incrementarse
	 * Pero la diferencia ahora esta el tamaño del contador j
	 * Este esta ajustado al del contador i,esto quiere decir que su tamaño dependera de i.
	 
	 * Tiempo de Complejidad:
	 * Es O(n^2) ya que se tarda un tiempo de n por cada for, al ser anidados se multiplican.
	 * **/
}
