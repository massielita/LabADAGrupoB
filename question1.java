
public class question1{
	/*Codigo:
      for(i=0;i<n;i++) {
    	//statement
      }
    */
	
	/**
	 * Descripcion:
	 * Esta linea de codigo es usada normalmente en ciclos
	 * El contador i siempre inicia en 0, siempre es menor que el dato con el que se trabaja
	 * Al tener simbolo de adicion este contador se esta incrementando
	 * Esto quiere decir que en cada interaccion el contador se incrementa de 1 en 1
	 
	 * Tiempo de Complejidad:
	 * Es O(n)ya que solo se tien un for en el codigo asi que seria llamado como maximo n veces.
	 * **/
}
