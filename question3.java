public class question3{
	/*Codigo:
      for(i=0;i<n;i=i+2) {
    	//statement
      }
    */
	
	/**
	 * Descripcion:
	 * La linea nos indica que ahora el incremento es de 2
	 * Esto quiere decir que en cada interaccion el contador se incrementa de 2 en 2
	 * Esto normalmente es usado en problema que tengan como tema a numeros pares, primos.
	 
	 * Tiempo de Complejidad:
	 * Es O(n)el incremento es 2 solo afecta al contador mas no a la cantidad de interacciones que tenga el programa
	 * **/
}
